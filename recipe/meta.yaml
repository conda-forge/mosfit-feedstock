{% set name = "mosfit" %}
{% set version = "1.3" %}
{% set sha256 = "a209c15bc7987df5eb2c6eb063ba71ce9c64a41b865cb7b273c389c0ec0caa02" %}

package:
  name: {{ name|lower }}
  version: "{{ version }}"

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  skip: true  # [py >= 313] -- because we require Numpy < 2

requirements:
  host:
    - python
    - pip
    - setuptools
    - astrocats
    - astropy-base
    - cython
    - dynesty
    - emcee
    - extinction
    - h5py
    - inflect
    - matplotlib-base
    - mpi4py  # [not win]
    - nbstripout
    - numexpr
    - numpy <=1.26.4
    - palettable
    - pandas >=2.1,<3
    - pytorch
    - schwimmbad <0.4
    - scipy
    - seaborn
    - six
  run:
    - python
    - astrocats
    - astropy-base
    - cython
    - dynesty
    - emcee
    - extinction
    - h5py
    - inflect
    - matplotlib-base
    - mpi4py  # [not win]
    - nbstripout
    - numexpr
    - numpy <=1.26.4
    - palettable
    - pandas >=2.1,<3
    - pytorch
    - schwimmbad <0.4
    - scipy
    - seaborn
    - six

test:
  requires:
    - git
    - openssh  # [not win]
    - xorg-libx11
  imports:
    - mosfit
  files:
    - forge_test.sh
  source_files:
    - test.py
    - test.sh
    - mosfit/tests/LSQ12dlf.json
    - mosfit/tests/SN2006le.json
    - mosfit/tests/event_list.txt
    - mosfit/tests/PTF10hgi.txt
  commands:
    - time bash ./forge_test.sh  # [not win]

about:
  home: https://github.com/guillochon/mosfit
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Modular fitter for astrophysical transients

  description: |
    mosfit is a package that performs fitting of semi-analytical model predictions
    to observed astrophysical transients. Data can be provided by the user or pulled
    automatically from the Open Astronomy catalogs.
  doc_url: https://mosfit.readthedocs.io/
  dev_url: https://github.com/guillochon/mosfit

extra:
  recipe-maintainers:
    - gmzsebastian
    - guillochon
    - mnicholl
    - pkgw
